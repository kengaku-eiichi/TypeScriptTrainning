{
    type FooType = [string, number, boolean]
    // let data: FooType = ['abc', '1', true]
    let data: FooType = ['abc', 1, true]

    type Hoge = string | boolean
    let hoge: Hoge = 'hoge'
    let fuga: Hoge = true
    // let one: Hoge = 1
}