{
    let triangle = (base: number, height: number) => { return base * height / 2 }
    let rectangle = (width: number, height: number) => width * height

    console.log(triangle(10, 5))
    console.log(rectangle(10, 5))
}